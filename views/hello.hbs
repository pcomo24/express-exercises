<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>{{title}}</title>
  </head>
  <body>
    <h1>{{title}} {{name}}</h1>
    <ul>
      {{#each friends}}
      <li>{{name}}</li>
      {{/each}}
    </ul>
  </body>
</html>
